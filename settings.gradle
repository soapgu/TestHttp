pluginManagement {
    repositories {
        maven { url 'https://repository.jboss.org/nexus/content/repositories/thirdparty-releases/' }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url 'https://repository.jboss.org/nexus/content/repositories/thirdparty-releases/' }
        google()
        mavenCentral()
    }
}
rootProject.name = "Test Http"
include ':app'
